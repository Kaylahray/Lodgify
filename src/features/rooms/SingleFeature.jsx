import React from "react";

const SingleFeature = ({text}) => {
  return <div className="flex gap-2">
    <div><img src="" alt="" />
    <p>{text}</p>
    </div>
  </div>;
};

export default SingleFeature;
